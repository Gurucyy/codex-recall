{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for read-only local Codex session recovery app.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
